apply plugin: 'com.android.application'

android {
    compileSdkVersion 4
    buildToolsVersion "21.1.0"

    defaultConfig {
        applicationId "com.androidbook.services.httpget"
        minSdkVersion 4
        targetSdkVersion 4
    }

    buildTypes {
        release {
            minifyEnabled true
            //runProguard false
            //proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}
