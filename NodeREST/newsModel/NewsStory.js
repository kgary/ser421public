var NewsStory = function(title, content, author, isPublic, date) {
    this.title = title,
    this.content = content,
    this.author = author,
    this.isPublic = isPublic,
    this.date = date;
}

module.exports = NewsStory;