These examples use the NodeJS platform, specifically file I/O through the fs module. As such they won't run in the browser. You can run them via Node, or just inspect them as you can see examples of callback h*ll, and how Promises and async/await can help resolve them.

